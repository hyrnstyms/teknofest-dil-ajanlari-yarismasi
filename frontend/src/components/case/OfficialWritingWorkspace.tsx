import React, { useEffect, useMemo, useState } from "react";
import { CheckCircle2, Download, FilePenLine, LockKeyhole, QrCode, RefreshCw, Save, ShieldCheck } from "lucide-react";
import type { CaseDraft, CaseRecord } from "../../types/case";
import { caseApi } from "../../services/caseApi";
import { caseDownload } from "../../services/caseHttp";

const typeLabels: Record<string, string> = { MISSING_INFORMATION_REQUEST: "Eksik Bilgi Talep Yazısı", INTERIM_INFORMATION: "Ara Bilgilendirme", OFFICIAL_RESPONSE: "Resmî Cevap", INTERNAL_MEMO: "Kurum İçi Yazışma", FORWARDING_COVER_LETTER: "İç Yönlendirme Üst Yazısı" };
const statusLabels: Record<string, string> = { DRAFT: "Taslak", EDITED: "Düzenlendi", APPROVED: "Onaylandı", SENT: "Gönderildi", CANCELLED: "İptal" };
const recipientKindLabels: Record<string, string> = { VATANDAS: "Başvuru Sahibi", KURUM: "Gönderen Kurum", DIS_KURUM: "Gönderen Kurum", INTERNAL_UNIT: "Kurum İçi Birim", INTERNAL_DEPARTMENT: "Kurum İçi Birim" };

export function OfficialWritingWorkspace({ item, token, onRefresh, onNotice }: { item: CaseRecord; token: string; onRefresh: () => Promise<void>; onNotice: (message: string) => void }) {
  const [selectedId, setSelectedId] = useState<string>(); const [editing, setEditing] = useState(false); const [busy, setBusy] = useState(""); const [error, setError] = useState("");
  const drafts = item.drafts; useEffect(() => { if (!selectedId || !drafts.some((d) => d.id === selectedId)) setSelectedId(drafts.at(-1)?.id); }, [drafts, selectedId]);
  const selected = drafts.find((d) => d.id === selectedId); const [form, setForm] = useState({ subject: "", recipient: "", body: "" });
  useEffect(() => { if (selected) setForm({ subject: selected.subject, recipient: selected.recipient || "", body: selected.body }); }, [selected]);
  const canEdit = Boolean(selected && selected.draft_status !== "APPROVED" && item.permissions.includes("SAVE_DRAFT"));
  async function action(name: string, operation: () => Promise<unknown>, message: string) { setBusy(name); setError(""); try { await operation(); await onRefresh(); onNotice(message); setEditing(false); } catch (e) { setError(e instanceof Error ? e.message : "İşlem tamamlanamadı."); } finally { setBusy(""); } }
  const lockedMessage = useMemo(() => { if (["RECEIVED", "ANALYZING", "WAITING_INITIAL_REVIEW", "READY_TO_ROUTE", "WAITING_CITIZEN_INFO"].includes(item.workflow_status)) return "Bu dosya henüz ilgili birime yönlendirilmedi. Eksik bilgi veya iç yönlendirme yazısı süreç aşamasına göre burada görünür."; if (item.workflow_status === "IN_DEPARTMENT") return `Dosya ${item.current_department_name} birimine yönlendirildi. Nihai cevap için birimin doğrulanmış işlem sonucu gereklidir.`; if (item.workflow_status === "IN_PROGRESS") return "Resmî cevap taslağı için önce kurum işlem sonucunu kaydedin."; return "Bu aşamada oluşturulmuş bir resmî yazı bulunmuyor."; }, [item]);
  return <section className="case-panel official-writing-workspace"><header><div><span className="eyebrow">TEKNOFEST GÖREV 2</span><h2>Resmî Yazılar <b>({drafts.length})</b></h2><p>Yazı türü, doğru muhatap, personel düzeltmesi ve onay aynı vaka kaydında izlenir.</p></div></header>
    {drafts.length > 0 && <div className="writing-layout"><nav className="writing-list">{drafts.map((draft) => <button key={draft.id} className={selected?.id === draft.id ? "active" : ""} onClick={() => { setSelectedId(draft.id); setEditing(false); }}><small>{typeLabels[draft.draft_type]}</small><strong>{draft.subject}</strong><span>{draft.sender_unit || item.current_department_name} → {draft.recipient || item.originator_name}</span><em>{statusLabels[draft.draft_status]} · v{draft.revision || 1}</em></button>)}</nav><div className="writing-detail">{selected && <><div className="human-control-strip"><span className="done">AI Taslağı</span><i>→</i><span className={selected.draft_status === "EDITED" || selected.draft_status === "APPROVED" ? "done" : ""}>Personel Düzenlemesi</span><i>→</i><span className={selected.draft_status === "APPROVED" ? "done" : ""}>Personel Onayı</span></div>{editing ? <div className="case-draft-editor"><label>Konu<input value={form.subject} onChange={(e) => setForm({ ...form, subject: e.target.value })}/></label><label>Muhatap<input value={form.recipient} onChange={(e) => setForm({ ...form, recipient: e.target.value })}/></label><label>Gövde<textarea rows={12} value={form.body} onChange={(e) => setForm({ ...form, body: e.target.value })}/></label><div><button className="btn btn-secondary" onClick={() => setEditing(false)}>İptal</button><button className="btn btn-primary" disabled={Boolean(busy)} onClick={() => void action("edit", () => caseApi.editDraft(token, item, selected, form), "Personel düzeltmesi yeni sürüm olarak kaydedildi.")}><Save size={16}/> Düzenlemeyi Kaydet</button></div></div> : <OfficialDocumentPreview item={item} draft={selected}/>}<div className="quality-checks"><strong><ShieldCheck/> Düzeltme / Kalite Kontrolü</strong><span>✓ Muhatap açıkça tanımlı</span><span>✓ Resmî üslup ve yapı</span><span>✓ Vaka ile aynı kurum bağlamı</span><span>{selected.grounded_action_id ? "✓ Doğrulanmış kurum işlemine dayalı" : "✓ İç süreç yazısı; nihai işlem iddiası içermez"}</span></div><div className="draft-actions">{canEdit && <button className="btn btn-secondary" onClick={() => setEditing(true)}><FilePenLine size={16}/> Düzenle</button>}{selected.draft_type === "OFFICIAL_RESPONSE" && selected.draft_status !== "APPROVED" && item.department_actions.length > 0 && <button className="btn btn-secondary" disabled={Boolean(busy)} onClick={() => void action("regen", () => caseApi.regenerateDraft(token, item), "Yeni AI taslak sürümü oluşturuldu.")}><RefreshCw size={16}/> Yeniden Oluştur</button>}{selected.draft_status !== "APPROVED" && item.permissions.includes("APPROVE_DRAFT") && <button className="btn btn-primary" disabled={Boolean(busy)} onClick={() => void action("approve", () => caseApi.approveDraft(token, item, selected.id), "Resmî yazı personel tarafından onaylandı.")}><CheckCircle2 size={16}/> Onayla</button>}<button className="btn btn-secondary" disabled={selected.draft_status !== "APPROVED" || Boolean(busy)} onClick={() => void caseDownload(`/api/cases/${item.id}/drafts/${selected.id}/export/docx`, token, `${item.tracking_code}.docx`)}><Download size={16}/> DOCX</button><button className="btn btn-secondary" disabled={selected.draft_status !== "APPROVED" || Boolean(busy)} onClick={() => void caseDownload(`/api/cases/${item.id}/drafts/${selected.id}/export/pdf`, token, `${item.tracking_code}.pdf`)}><Download size={16}/> PDF</button></div></>}</div></div>}
    {!drafts.length && <div className="writing-locked"><LockKeyhole/><div><strong>Resmî yazı alanı hazır</strong><p>{lockedMessage}</p><span>Kurum İşlemi → Taslak → Düzenleme → Onay → Çıktı</span></div></div>}{busy === "regen" && <div className="case-success">Resmî cevap taslağı hazırlanıyor…</div>}{error && <div className="case-error">{error}</div>}
  </section>;
}

export function OfficialDocumentPreview({ item, draft }: { item: CaseRecord; draft: CaseDraft }) { return <article className="case-a4 official-document-preview"><header><strong>T.C.</strong><strong>{item.institution_id === "belediye" ? "BELEDİYE BAŞKANLIĞI" : "KAYMAKAMLIK"}</strong><span>{draft.sender_unit || item.current_department_name}</span></header><div className="document-number-date"><span><b>Sayı:</b> [EBYS SAYISI]</span><span>{new Date(draft.updated_at || item.updated_at).toLocaleDateString("tr-TR")}</span></div><p><b>Konu:</b> {draft.subject}</p><p><b>Muhatap:</b> {recipientKindLabels[draft.recipient_kind || ""] || draft.recipient_kind || "Belirtilmedi"}</p><h3>{draft.recipient || item.originator_name}</h3>{draft.body.split("\n").filter(Boolean).map((line, index) => <p key={index}>{line}</p>)}<div className="document-signature"><b>[AD SOYAD]</b><span>[UNVAN]</span></div><div className="case-qr"><QrCode/><small>{item.tracking_code}<br/>EVRAG doğrulama kaydı</small></div></article>; }
