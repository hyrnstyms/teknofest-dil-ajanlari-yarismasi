import React, { useEffect, useState } from "react";
import { Check, Circle, Clock3, FileCheck2, ShieldCheck } from "lucide-react";
import { useParams, useSearchParams } from "react-router-dom";
import { EVRAGBrand } from "../components/EVRAGBrand";
import { publicCaseApi } from "../services/publicCaseApi";
import type { PublicCase } from "../types/case";
export function CitizenTracePage() { const { trackingCode = "" } = useParams(); const [params] = useSearchParams(); const token = params.get("token") || ""; const [item, setItem] = useState<PublicCase>(); const [values, setValues] = useState<Record<string, string>>({}); const [error, setError] = useState(""); const [submitted, setSubmitted] = useState(false); const [busy, setBusy] = useState(false); useEffect(() => { if (!token) { setError("Güvenli takip bağlantısı eksik veya geçersiz."); return; } void publicCaseApi.get(trackingCode, token).then(setItem).catch((e) => setError(e.message)); }, [trackingCode, token]); async function submit(e: React.FormEvent) { e.preventDefault(); setBusy(true); setError(""); try { const next = await publicCaseApi.completeInfo(trackingCode, token, values); setItem(next); setSubmitted(true); } catch (err) { setError(err instanceof Error ? err.message : "Bilgi gönderilemedi."); } finally { setBusy(false); } } return <main className="citizen-page"><header><EVRAGBrand variant="full" theme="light"/><span><ShieldCheck/> Güvenli Başvuru Takibi</span></header><div className="citizen-shell"><section className="citizen-intro"><span className="eyebrow">BAŞVURU TAKİP NUMARASI</span><h1>{trackingCode}</h1><p>Bu sayfa yalnız başvurunuzun güvenli ve sade süreç bilgisini gösterir.</p></section>{error && <div className="case-error" role="alert">{error}</div>}{!error && !item && <div className="case-loading">Başvuru bilgileri güvenli biçimde alınıyor…</div>}{item && <><section className="citizen-summary"><div><small>Başvuru konusu</small><strong>{item.subject}</strong></div><div><small>Alınma tarihi</small><strong>{new Date(item.received_at).toLocaleDateString("tr-TR")}</strong></div><div><small>Güncel durum</small><strong>{item.status}</strong></div><div><small>Son güncelleme</small><strong>{new Date(item.updated_at).toLocaleDateString("tr-TR")}</strong></div></section><section className="citizen-progress"><h2>Başvurunuzun yolculuğu</h2><ol>{item.timeline.map((step) => <li className={step.state} key={step.id}><span>{step.state === "completed" ? <Check/> : step.state === "current" ? <Clock3/> : <Circle/>}</span><strong>{step.label}</strong></li>)}</ol></section>{item.workflow_status === "WAITING_CITIZEN_INFO" && item.clarification && !submitted && <section className="citizen-info-form"><span><FileCheck2/></span><div><span className="eyebrow">İŞLEME DEVAM ETMEK İÇİN</span><h2>Bir bilginizi tamamlamanız gerekiyor</h2><p>{item.clarification.question}</p><form onSubmit={(e) => void submit(e)}>{item.clarification.requested_fields.map((field) => <label key={field}>{field === "location" ? "Açık adres" : field === "permit_type" ? "Ruhsat türü" : "İstenen bilgi"}{item.clarification?.question_type === "single_choice" ? <select required value={values[field] || ""} onChange={(e) => setValues({ ...values, [field]: e.target.value })}><option value="">Seçiniz</option>{item.clarification.options.map((o) => <option value={o.value} key={o.value}>{o.label}</option>)}</select> : <input required value={values[field] || ""} onChange={(e) => setValues({ ...values, [field]: e.target.value })}/>}</label>)}<button className="btn btn-primary" disabled={busy}>{busy ? "Gönderiliyor…" : "Bilgiyi güvenli biçimde gönder"}</button></form></div></section>}{submitted && <section className="citizen-success"><Check/><div><h2>Bilginiz alındı</h2><p>Başvurunuz yeniden inceleme sırasına aktarıldı. Aynı bağlantıdan güncel durumu takip edebilirsiniz.</p></div></section>}<p className="citizen-privacy">Bu ekranda kurum içi notlar, AI değerlendirme ayrıntıları veya başka kişilere ait bilgiler gösterilmez.</p></>}</div></main>; }
