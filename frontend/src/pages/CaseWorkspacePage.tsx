import React, { useEffect, useState } from "react";
import { Bot, Building2, CalendarClock, ChevronRight, FileText, LockKeyhole, Route, Save, UserRound } from "lucide-react";
import { Link, useParams } from "react-router-dom";
import { useAuth } from "../contexts/AuthContext";
import { caseApi } from "../services/caseApi";
import type { CaseRecord, DepartmentAction } from "../types/case";
import { CaseTimeline, ConfirmAction, StatusBadge } from "../components/case/CasePrimitives";
type Pending = "route" | "start" | "clarification" | null;
const blankAction = { action_type: "", result: "", decision: "", planned_date: "", notes: "" };
export function CaseWorkspacePage() { const { id = "" } = useParams(); const { token, user } = useAuth(); const [item, setItem] = useState<CaseRecord>(); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [notice, setNotice] = useState(""); const [pending, setPending] = useState<Pending>(null); const [busy, setBusy] = useState(false); const [form, setForm] = useState(blankAction); useEffect(() => { if (!token) return; void caseApi.get(token, id).then(setItem).catch((e) => setError(e.message)).finally(() => setLoading(false)); }, [token, id]); async function confirm() { if (!token || !user || !item || !pending) return; setBusy(true); setError(""); try { const result = pending === "route" ? await caseApi.route(token, item, item.routing_recommendation!.recommended_department_code) : pending === "start" ? await caseApi.start(token, item) : await caseApi.requestCitizenInfo(token, item); setItem(result.case); setNotice(result.message); setPending(null); } catch (e) { setError(e instanceof Error ? e.message : "İşlem tamamlanamadı."); } finally { setBusy(false); } } async function saveAction(e: React.FormEvent) { e.preventDefault(); if (!token || !user || !item) return; setBusy(true); setError(""); try { const result = await caseApi.departmentAction(token, item, form as Omit<DepartmentAction, "id" | "verified" | "recorded_by_user_id" | "created_at">); setItem(result.case); setNotice("Doğrulanmış kurum işlem sonucu kaydedildi."); setForm(blankAction); } catch (err) { setError(err instanceof Error ? err.message : "İşlem sonucu kaydedilemedi."); } finally { setBusy(false); } } if (loading) return <div className="case-loading">Dosya ve yetkiler yükleniyor…</div>; if (error && !item) return <div className="case-page"><div className="case-error" role="alert">{error}<small>Bu dosya kurum veya birim yetkinizin dışında olabilir.</small></div></div>; if (!item) return null; const canRoute = item.permissions.includes("ROUTE_CASE") && user?.role === "EVRAK_KAYIT"; const canStart = item.permissions.includes("START_CASE") && user?.role === "BIRIM_PERSONELI"; const canAction = item.permissions.includes("RECORD_DEPARTMENT_ACTION") && user?.role === "BIRIM_PERSONELI"; const closed = item.workflow_status === "CLOSED"; const verifiedAction = item.department_actions.some((x) => x.verified); return <div className="case-page case-workspace"><nav className="case-breadcrumb"><Link to="/dosyalar">Dosyalar</Link><ChevronRight/><span>{item.tracking_code}</span></nav><header className="case-workspace-header"><div><span className="eyebrow">{item.tracking_code}</span><h1>{item.title}</h1><StatusBadge status={item.workflow_status}/></div><div className="ownership-strip"><section><span><UserRound/> Kaynak / Başvuru Sahibi</span><strong>{item.originator_name}</strong><small>{item.source_type} · {item.source_channel === "EBYS" ? "Demo Evrak Girişi" : item.source_channel}</small></section><section><span><Building2/> Mevcut Sahip</span><strong>{item.current_department_name}</strong><small>Kurumsal sorumluluk</small></section><section className="ai-owner"><span><Bot/> AI Önerisi</span><strong>{item.routing_recommendation?.recommended_unit || "Öneri bulunmuyor"}</strong><small>İnsan onayı olmadan sahiplik değişmez</small></section></div></header>{notice && <div className="case-success" role="status">{notice}</div>}{error && <div className="case-error" role="alert">{error}</div>}<div className="case-workspace-grid"><main><section className="case-panel"><header><div><span className="eyebrow">AI ANALİZİ</span><h2>Başvuru özeti</h2></div></header><p>{item.analysis_summary || "Analiz özeti henüz hazır değil."}</p></section>{item.clarification?.needs_clarification && <section className="case-panel clarification-panel"><header><div><span className="eyebrow">BLOKE EDİCİ EKSİK BİLGİ</span><h2>Eksik bilgi nedeniyle bekliyor</h2></div></header><label>AI Önerilen Soru</label><blockquote>{item.clarification.question}</blockquote><p>Yalnız işleme devam etmek için gereken minimum bilgi istenir.</p>{item.permissions.includes("REQUEST_CITIZEN_INFO") && <button className="btn btn-primary" disabled={closed} onClick={() => setPending("clarification")}>Vatandaştan Bilgi İste</button>}</section>}{item.routing_recommendation && <section className="case-panel routing-decision"><header><div><span className="eyebrow">AI ÖNERİSİ · İNSAN KARARI GEREKİR</span><h2>{item.routing_recommendation.recommended_unit}</h2></div><Route/></header><h3>Gerekçe</h3><p>{item.routing_recommendation.reason}</p>{item.routing_recommendation.evidence.length > 0 && <><h3>Kanıt</h3><ul>{item.routing_recommendation.evidence.map((x) => <li key={x}>{x}</li>)}</ul></>}{item.routing_recommendation.alternatives.length > 0 && <><h3>Alternatif Birimler</h3><ul>{item.routing_recommendation.alternatives.map((x) => <li key={x.department_code}>{x.unit}{x.reason ? ` — ${x.reason}` : ""}</li>)}</ul></>}{canRoute && <button className="btn btn-primary" disabled={closed} onClick={() => setPending("route")}>{item.routing_recommendation.recommended_unit} birimine yönlendir</button>}</section>}{canAction && <section className="case-panel"><header><div><span className="eyebrow">İNSAN TARAFINDAN DOĞRULANAN KAYNAK</span><h2>Kurum işlem sonucunu kaydet</h2></div></header><p>Bu bilgiler yapay zekânın resmî cevap taslağında kullanacağı doğrulanmış kurum işlem kaynağıdır.</p><form className="department-action-form" onSubmit={(e) => void saveAction(e)}><label>İşlem Türü<input required value={form.action_type} onChange={(e) => setForm({ ...form, action_type: e.target.value })} placeholder="Örn. Saha incelemesi"/></label><label>Sonuç<textarea required value={form.result} onChange={(e) => setForm({ ...form, result: e.target.value })}/></label><label>Karar<textarea required value={form.decision} onChange={(e) => setForm({ ...form, decision: e.target.value })}/></label><label>Planlanan Tarih<input type="date" value={form.planned_date} onChange={(e) => setForm({ ...form, planned_date: e.target.value })}/></label><label className="full">Personel Notu<textarea value={form.notes} onChange={(e) => setForm({ ...form, notes: e.target.value })}/></label><button className="btn btn-primary" disabled={busy}><Save size={16}/> Doğrulanmış işlem sonucunu kaydet</button></form></section>}<section className="case-panel draft-guard"><header><div><span className="eyebrow">TASLAKLAR</span><h2>Resmî cevap hazırlığı</h2></div><FileText/></header>{verifiedAction ? <><p>Doğrulanmış kurum işlemi mevcut. Backend desteğiyle resmî cevap önizlemesi oluşturulabilir.</p><button className="btn btn-primary" disabled={!item.permissions.includes("CREATE_DRAFT")}>Cevap Taslağı Oluştur</button></> : <div className="guard-message"><LockKeyhole/><div><strong>Önce kurum işlem sonucu kaydedilmelidir.</strong><p>AI, gerçekleşmemiş bir kamu işlemi adına nihai cevap oluşturamaz.</p></div></div>}</section></main><aside><section className="case-panel next-action"><span className="eyebrow">SONRAKİ ADIM</span><h2>{closed ? "Dosya kapatıldı" : canRoute ? "Yönlendirmeyi doğrulayın" : canStart ? "Dosyayı işleme alın" : canAction ? "Kurum işlemini kaydedin" : "Mevcut aşamayı inceleyin"}</h2>{canStart && <button className="btn btn-primary" onClick={() => setPending("start")}>İşleme Al</button>}</section>{item.deadline?.applicable && item.deadline.legal_basis?.verified ? <section className="case-panel deadline-card"><span><CalendarClock/> Yasal Süre</span><strong>{item.deadline.deadline_days} gün</strong><dl><div><dt>Son tarih</dt><dd>{item.deadline.due_at ? new Date(item.deadline.due_at).toLocaleDateString("tr-TR") : "—"}</dd></div><div><dt>Risk</dt><dd>{item.deadline.risk_level}</dd></div><div><dt>Dayanak</dt><dd>{item.deadline.legal_basis.citation}</dd></div></dl></section> : <section className="case-panel"><p>Doğrulanmış süre bulunamadı.</p></section>}<section className="case-panel"><header><div><span className="eyebrow">İŞLEM GEÇMİŞİ</span><h2>Dosya zaman çizelgesi</h2></div></header><CaseTimeline events={item.timeline}/></section></aside></div>{pending && <ConfirmAction busy={busy} title={pending === "route" ? "Kurumsal sorumluluğu aktar" : pending === "start" ? "Dosyayı işleme al" : "Eksik bilgi talebini gönder"} text={pending === "route" ? `Bu işlem dosyanın kurumsal sorumluluğunu ${item.routing_recommendation?.recommended_unit} birimine aktaracaktır.` : pending === "start" ? "Bu işlem dosyayı biriminiz adına işlemde durumuna getirecektir." : "Önerilen minimum bilgi sorusu vatandaşa iletilecektir."} onCancel={() => setPending(null)} onConfirm={() => void confirm()}/>}</div>; }
